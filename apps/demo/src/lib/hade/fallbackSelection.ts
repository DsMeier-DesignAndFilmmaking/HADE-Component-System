/** @deprecated Import from `@hade/core` — compatibility shim for /demo and API routes. */
export type { FallbackEntryLike } from "@hade/core";
export {
  extractRejectedVenueIds,
  extractSurfacedFallbackTitles,
  recoverLeastRecentlySurfaced,
  sortFallbackCandidates,
} from "@hade/core";
