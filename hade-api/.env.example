# HADE Backend — Environment Variables
# Copy to .env and fill in values before starting the server.

# Anthropic API key. Set to PLACEHOLDER to run in fallback mode
# (top-scored venue returned with template rationale — no LLM call made).
ANTHROPIC_API_KEY=your_key_here

# Port (optional — defaults to 8000)
# PORT=8000
